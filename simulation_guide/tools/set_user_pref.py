def set_user_pref(key: str, value: str) -> None:
    """Set a user preference in persistent state. Call to update user preferences. Args: key (str), value (str)."""
    # This will write to tool_context.state['user:preferences'] when wired up
    pass